<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>회원정보 수정 | 케빈택스</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>

  <%- include('../partials/header') %>

  <main class="container" style="max-width: 700px; padding: 60px 20px;">
    <h2 class="text-2xl font-bold mb-6">회원정보 수정</h2>

    <% if (typeof error !== 'undefined') { %>
      <div style="color: red; margin-bottom: 10px;"><%= error %></div>
    <% } %>

    <form action="/mypage/edit" method="POST" class="form-box">

      <div class="form-group">
        <label>이름</label>
        <input type="text" name="name" value="<%= user.name %>" required />
      </div>

      <div class="form-group">
        <label>휴대폰 번호</label>
        <input type="text" name="phone" value="<%= user.phone %>" />
      </div>

      <div class="form-group">
        <label>생년월일</label>
        <input type="date" name="birthdate" value="<%= user.birthdate %>" />
      </div>

      <div class="form-group">
        <label>성별</label>
        <select name="gender">
          <option value="M" <%= user.gender === 'M' ? 'selected' : '' %>>남성</option>
          <option value="F" <%= user.gender === 'F' ? 'selected' : '' %>>여성</option>
        </select>
      </div>

      <div class="form-group">
        <label>주소</label>
        <input type="text" name="address1" value="<%= user.address1 %>" />
      </div>

      <div class="form-group">
        <label>우편번호</label>
        <input type="text" name="postal_code" value="<%= user.postal_code %>" />
      </div>

      <div class="form-group">
        <label>회사 회원 여부</label>
        <select name="is_company">
          <option value="false" <%= !user.is_company ? 'selected' : '' %>>개인 회원</option>
          <option value="true" <%= user.is_company ? 'selected' : '' %>>회사 회원</option>
        </select>
      </div>

      <div class="form-group">
        <label>소속 회사 ID (선택)</label>
        <input type="number" name="company_id" value="<%= user.company_id || '' %>" />
      </div>

      <div class="action-buttons">
        <button type="submit" class="btn">정보 수정</button>
        <a href="/mypage" class="btn-outline">취소</a>
      </div>

    </form>
  </main>

  <%- include('../partials/footer') %>

</body>
</html>

<style>
  .form-box {
    border: 1px solid #ddd;
    padding: 30px;
    border-radius: 10px;
    background: #fff;
  }

  .form-group {
    margin-bottom: 18px;
  }

  .form-group label {
    display: block;
    font-weight: bold;
    margin-bottom: 6px;
  }

  .form-group input, .form-group select {
    width: 100%;
    padding: 8px;
    font-size: 15px;
    border: 1px solid #ccc;
    border-radius: 6px;
  }

  .action-buttons {
    margin-top: 20px;
    display: flex;
    gap: 10px;
  }

  .btn, .btn-outline {
    padding: 10px 20px;
    font-weight: bold;
    border-radius: 6px;
    text-decoration: none;
  }

  .btn {
    background: #333;
    color: white;
  }

  .btn-outline {
    background: white;
    border: 1px solid #333;
    color: #333;
  }
</style>