<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>마이페이지 | 케빈택스</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>

  <%- include('../partials/header') %>

  <main class="container" style="max-width: 720px; padding: 60px 20px;">
    <h2 class="text-2xl font-bold mb-6">마이페이지</h2>
    
    <div class="card" style="padding: 30px; border: 1px solid #ddd; border-radius: 10px;">
      <div class="info-row"><span>이름</span><span><%= user.name %></span></div>
      <div class="info-row"><span>이메일</span><span><%= user.email %></span></div>
      <div class="info-row"><span>휴대폰 번호</span><span><%= user.phone %></span></div>
      <div class="info-row"><span>성별</span><span><%= user.gender === 'M' ? '남성' : '여성' %></span></div>
      <div class="info-row"><span>생년월일</span><span><%= user.birthdate %></span></div>
      <div class="info-row"><span>회사 회원 여부</span><span><%= user.is_company ? '회사 회원' : '개인 회원' %></span></div>
      <div class="info-row"><span>주소</span><span><%= user.address1 || '-' %></span></div>
      <div class="info-row"><span>우편번호</span><span><%= user.postal_code || '-' %></span></div>
      <div class="info-row"><span>가입일</span><span><%= user.created_at?.split('T')[0] %></span></div>
      <div class="info-row"><span>최근 로그인</span><span><%= user.last_login_at?.split('T')[0] || '-' %></span></div>

      <div class="action-buttons" style="margin-top: 30px; display: flex; gap: 10px;">
        <a href="/mypage/edit" class="btn">회원 정보 수정</a>
        <a href="/logout" class="btn-outline">로그아웃</a>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>

</body>
</html>

<style>
  .info-row {
    display: flex;
    justify-content: space-between;
    padding: 12px 0;
    border-bottom: 1px solid #eee;
    font-size: 16px;
  }

  .info-row span:first-child {
    font-weight: 600;
    color: #333;
  }

  .btn, .btn-outline {
    padding: 10px 18px;
    border-radius: 6px;
    text-decoration: none;
    font-weight: bold;
  }

  .btn {
    background: #444;
    color: white;
  }

  .btn-outline {
    border: 1px solid #888;
    color: #444;
    background: white;
  }
</style>